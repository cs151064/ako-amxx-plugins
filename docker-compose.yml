services:
  fastdl:
    image: nginx:1.30-alpine
    container_name: cs16-fastdl
    restart: unless-stopped
    ports:
      - "27017:80/tcp"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - D:/CounterStrike/hlds:/hlds:ro
